<form id="edit-location-form" action="" method="post" class="container-form">
  <!--prettier-ignore-->
  <%- include('../components/form-hidden-input') %>
  <%- include('../components/form-errors') %>
  <div class="input-select">
    <label class="style-required" for="parentId">Location:</label>
    <select id="newParentId" name="parentId" class="">
      <option value="" selected>Home</option>
      <hr />
      <optgroup label="Recent Folders">
        <% folderList.forEach(folder => { %>
        <option value="<%= folder.id %>"><%= folder.name %></option>
        <% }); %>
      </optgroup>
    </select>
  </div>

  <div class="button-group">
    <button
      data-modal-hide="<%= modalId %>"
      type="button"
      class="button-cancel"
    >
      Cancel
    </button>
    <button type="submit" class="button-submit-neutral">Submit</button>
  </div>
</form>
